# Dependencias de los notebooks.
# Instalación (Linux/macOS):  python -m venv .venv && .venv/bin/pip install -r requirements.txt
# Instalación (Windows):      python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
#
# Fuera de pip: poppler (`pdftotext`, `pdfinfo`). Lo usan los notebooks 01, 02, 03 y 07.
# Con poppler >= 25 el notebook 07 compensa una "A" que esa versión descarta en las pp. 362-364.
#
# pandas 2.x a propósito: con pandas 3 las columnas de texto se graban como `str` en el
# Parquet en lugar de `object` y cambian los tipos publicados.
pandas==2.3.3
numpy==2.4.6
pyarrow==25.0.1
matplotlib==3.11.2
scipy==1.17.1

jupyterlab==4.6.3
ipykernel==7.3.0
nbformat==5.11.1
nbconvert==7.17.1
